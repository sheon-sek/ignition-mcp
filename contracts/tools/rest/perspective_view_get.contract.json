{
  "schemaVersion": 1,
  "name": "perspective_view_get",
  "server": "ignition-rest",
  "owner": "rest",
  "stability": "experimental",
  "operationKind": "read",
  "permissionClass": "READ",
  "requiredScope": "ignition.read",
  "mutationClass": "NONE",
  "budgetClass": "ARTIFACT",
  "parameters": {
    "projectName": {
      "type": "string",
      "required": true,
      "minLength": 1,
      "maxLength": 256
    },
    "path": {
      "type": "string",
      "required": true,
      "minLength": 1,
      "maxLength": 512,
      "logicalResourcePath": true
    }
  },
  "inputBounds": {
    "pathMaxBytes": 512,
    "pathRule": "a Logical resource path relative to the Views root; a leading '/', a backslash, an empty, '.' or '..' segment and any character Ignition refuses in a resource name are invalid_argument",
    "documentMaxBytes": 1048576,
    "overBudgetCode": "limit_exceeded"
  },
  "outputSchema": "contracts/schemas/perspective-view-get.output.schema.json",
  "nativeRequirements": [
    "GET /data/api/v1/projects/export/{name}"
  ],
  "capabilityId": "project_export",
  "localResourcesOnly": true,
  "inheritanceLookup": false,
  "preconditionTokenReturned": "the D16 pcf1 Project fingerprint of the export this document was read from",
  "embeddedSecretsRedacted": true,
  "exportHandling": "the export is streamed into private staging, read in place and never published as an artifact or passed through the MCP body (D15/D17)",
  "missingView": "not_found",
  "silentTruncation": false
}
