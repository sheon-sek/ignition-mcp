{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:ignition-mcp:rest:perspective-view-validate:output",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "correlationId": {"type": "string", "minLength": 1},
    "valid": {
      "description": "True for every returned result: an invalid document is refused with a D06 error code instead.",
      "type": "boolean"
    },
    "bytes": {
      "description": "Serialized document size, measured against the byte ceiling.",
      "type": "integer",
      "minimum": 0
    },
    "depth": {
      "description": "JSON values on the longest path from the document to a leaf, measured against the depth ceiling.",
      "type": "integer",
      "minimum": 0
    }
  },
  "required": ["correlationId", "valid", "bytes", "depth"]
}
