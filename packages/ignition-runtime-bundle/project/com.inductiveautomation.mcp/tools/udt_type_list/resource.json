{
  "scope": "G",
  "version": 1,
  "restricted": false,
  "overridable": false,
  "files": ["onToolCalled.py"],
  "attributes": {
    "title": "udt_type_list",
    "description": "List UDT type definitions in one Tag Provider without exposing the internal _types_ namespace in the public contract.",
    "parameters": [
      {"name": "provider", "description": "Tag Provider name without brackets.", "type": "string", "required": true},
      {"name": "maxResults", "description": "Maximum UDT types, 1..500. Default 100.", "type": "integer", "required": false, "default": 100}
    ]
  }
}
