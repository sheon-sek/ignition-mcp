{
  "redactions": {
    "apiTokens": "replaced by recorded:AAAAAAAAAAAAAAAAAAAAAAAAAAAA",
    "secrets": "no recorded secret values retained"
  },
  "schemaVersion": 1,
  "sources": {
    "http/forbidden.json": "run 35588029382 observations.json",
    "http/unsupported-media-type.json": "run 35589554998 observations.json",
    "mcp/initialize.json": "run 35529974781 raw/runtime-initialize.json",
    "mcp/prompts-list-invalid-request.json": "run 35588754132 observations.json",
    "phase2/certificate-already-accepted.json": "run 35524417843 provision.json",
    "phase2/credential-object-required.json": "run 35523317936 provision.json",
    "phase2/database-drivers.json": "databaseDriver from run 35527517320 provision.json",
    "phase2/database-translators.json": "databaseTranslator from run 35527517320 provision.json",
    "phase2/modules-healthy-after-restart.json": "healthyModuleIds from run 35524729067 provision-heal-after-restart.json",
    "phase2/modules-healthy-before-restart.json": "healthyModuleIds diagnostic from run 35524729067 provision-heal.json",
    "phase2/modules-quarantined.json": "module IDs and certificate state from run 35524729067 provision-heal.json",
    "phase2/no-route.json": "runs 35523626639 and 35524729067 provision.json",
    "phase2/openapi-required.json": "requiredEndpoints from run 35527517320 provision.json",
    "phase2/postgresql-missing.json": "run 35522848533 provision.json",
    "phase2/provision-success.json": "run 35527517320 provision.json",
    "phase2/tag-import-bare-array.json": "run 35527191629 provision.json",
    "phase4/alarm-probe.json": "recorded from phase4-live-g4a run 35641086552 (8.3.8 job 106463444618); the 8.3.9 row of the same run measured identically (alarm_probe handler report; its cycle counts are the evidence that a single exact Alarm path accumulates events without acknowledgement)",
    "phase4/alarm-shelve-allowlisted.json": "recorded from phase4-live-g4a run 35659773936 (8.3.8 job); the 8.3.9 row of the same run measured identically apart from the run's wall clock (the Alarm root and the correlation ID are the run's own, so both are templated: __ALARM_ROOT__ and __CORRELATION__) (the shipped alarm_shelve result for the run's exact Alarm path)",
    "phase4/alarm-shelve-cap-refusal.json": "recorded from phase4-live-g4a run 35659773936 (8.3.8 job); the 8.3.9 row of the same run measured identically apart from the run's wall clock (the Alarm root and the correlation ID are the run's own, so both are templated: __ALARM_ROOT__ and __CORRELATION__) (the shipped alarm_shelve refusal for a duration above the policy cap)",
    "phase4/alarm-shelve-hard-max-refusal.json": "recorded from phase4-live-g4a run 35659773936 (8.3.8 job); the 8.3.9 row of the same run measured identically apart from the run's wall clock (the Alarm root and the correlation ID are the run's own, so both are templated: __ALARM_ROOT__ and __CORRELATION__) (the shipped alarm_shelve refusal above the D12 24 h hard maximum)",
    "phase4/alarm-shelve-no-policy.json": "recorded from phase4-live-g4a run 35659773936 (8.3.8 job); the 8.3.9 row of the same run measured identically apart from the run's wall clock (the Alarm root and the correlation ID are the run's own, so both are templated: __ALARM_ROOT__ and __CORRELATION__) (the shipped alarm_shelve fail-closed refusal, before any policy was provisioned)",
    "phase4/alarm-shelve-preflight-refusal.json": "recorded from phase4-live-g4a run 35659773936 (8.3.8 job); the 8.3.9 row of the same run measured identically apart from the run's wall clock (the Alarm root and the correlation ID are the run's own, so both are templated: __ALARM_ROOT__ and __CORRELATION__) (the shipped alarm_shelve whole-batch Preflight refusal)",
    "phase4/alarm-shelve-sibling-denial.json": "recorded from phase4-live-g4a run 35659773936 (8.3.8 job); the 8.3.9 row of the same run measured identically apart from the run's wall clock (the Alarm root and the correlation ID are the run's own, so both are templated: __ALARM_ROOT__ and __CORRELATION__) (the shipped alarm_shelve segment-boundary refusal)",
    "phase4/alarm-shelve-wildcard-refusal.json": "recorded from phase4-live-g4a run 35659773936 (8.3.8 job); the 8.3.9 row of the same run measured identically apart from the run's wall clock (the Alarm root and the correlation ID are the run's own, so both are templated: __ALARM_ROOT__ and __CORRELATION__) (the shipped alarm_shelve refusal of a wildcard target)",
    "phase4/alarm-unshelve-allowlisted.json": "recorded from phase4-live-g4a run 35659773936 (8.3.8 job); the 8.3.9 row of the same run measured identically apart from the run's wall clock (the Alarm root and the correlation ID are the run's own, so both are templated: __ALARM_ROOT__ and __CORRELATION__) (the shipped alarm_unshelve result)",
    "phase4/alarm-unshelve-no-policy.json": "recorded from phase4-live-g4a run 35659773936 (8.3.8 job); the 8.3.9 row of the same run measured identically apart from the run's wall clock (the Alarm root and the correlation ID are the run's own, so both are templated: __ALARM_ROOT__ and __CORRELATION__) (the shipped alarm_unshelve fail-closed refusal, before any policy was provisioned)",
    "phase4/alarm-unshelve-sibling-denial.json": "recorded from phase4-live-g4a run 35659773936 (8.3.8 job); the 8.3.9 row of the same run measured identically apart from the run's wall clock (the Alarm root and the correlation ID are the run's own, so both are templated: __ALARM_ROOT__ and __CORRELATION__) (the shipped alarm_unshelve segment-boundary refusal)",
    "phase4/alarm-unshelve-wildcard-refusal.json": "recorded from phase4-live-g4a run 35659773936 (8.3.8 job); the 8.3.9 row of the same run measured identically apart from the run's wall clock (the Alarm root and the correlation ID are the run's own, so both are templated: __ALARM_ROOT__ and __CORRELATION__) (the shipped alarm_unshelve refusal of a wildcard target)",
    "phase4/audit-log-alarm.json": "recorded from phase4-live-g4a run 35659773936 (8.3.8 job); the two Runtime audit rows the alarm_shelve call wrote, read back through GET /data/api/v1/audit/log/<profile> (the action, tool name and correlation ID are templated)",
    "phase4/audit-log-config.json": "recorded from phase4-live-g4b run 35668653064 (8.3.8 job); the 8.3.9 row of the same run measured identically (the fingerprint of the same configuration and the same synthesized node), so both rows agree byte for byte apart from the run's own correlation IDs, which are templated (the two Runtime audit rows the allowlisted tag_update call wrote, read back through GET /data/api/v1/audit/log/<profile>; the action target and correlation ID are templated)",
    "phase4/audit-log-tag-config.json": "modelled from the shipped `system.util.audit` calls of `tag_create` and `tag_copy`: the two Runtime audit rows (attempt and result) one dispatched Mutation writes, read back through GET /data/api/v1/audit/log/<profile>; the action, tool name, action target and correlation ID are templated because they are the run's own, and the live G4b run re-records them from the Gateway (the same row pair the recorded ticket #10 `audit-log-config.json` carries)",
    "phase4/policy-probe-after-restart.json": "recorded from phase4-live-g4a run 35641086552 (8.3.8 job 106463444618); the 8.3.9 row of the same run measured identically (policy_probe handler report, after the Gateway restart)",
    "phase4/policy-probe.json": "recorded from phase4-live-g4a run 35641086552 (8.3.8 job 106463444618); the 8.3.9 row of the same run measured identically (policy_probe handler report, schemaVersion 2, before the Gateway restart)",
    "phase4/tag-config.json": "recorded from phase4-live-g4b run 35668653064 (8.3.8 job); the 8.3.9 row of the same run measured identically (the fingerprint of the same configuration and the same synthesized node), so both rows agree byte for byte apart from the run's own correlation IDs, which are templated (the Tag configuration the fake serves: the live `tag_get_config` read of [default]IgnitionMCP_CI/WriteTarget, and the Folder node of [default]IgnitionMCP_CI/Nested with the property the live merge added removed again, so it is that target's pre-update state; the TextTarget and sibling entries keep the ticket #7 probe shape, which no ticket #10 case read live)",
    "phase4/tag-copy-destination-exists.json": "modelled from the shipped `tag_copy` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the conflict refusal of an occupied destination)",
    "phase4/tag-copy-destination-leaf-mismatch.json": "modelled from the shipped `tag_copy` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the invalid_argument refusal of a destination whose leaf is not the source's)",
    "phase4/tag-copy-items-over-hard-limit.json": "modelled from the shipped `tag_copy` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the D10 ceiling refusal is a pure request check with no Gateway involvement, so no live run can record anything the handler's own message does not already state; the live G4b stage asserts the code, reason and quoted ceiling on both rows)",
    "phase4/tag-copy-no-policy.json": "modelled from the shipped `tag_copy` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the shipped `tag_copy` fail-closed refusal, before any policy was provisioned)",
    "phase4/tag-copy-over-policy-limit.json": "modelled from the shipped `tag_copy` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the D10 ceiling refusal is a pure request check with no Gateway involvement, so no live run can record anything the handler's own message does not already state; the live G4b stage asserts the code, reason and quoted ceiling on both rows)",
    "phase4/tag-copy-path-over-length.json": "modelled from the shipped `tag_copy` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the D10 ceiling refusal is a pure request check with no Gateway involvement, so no live run can record anything the handler's own message does not already state; the live G4b stage asserts the code, reason and quoted ceiling on both rows)",
    "phase4/tag-copy-preflight-refusal.json": "modelled from the shipped `tag_copy` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the whole-batch refusal that lists only the sibling and leaves the `_types_` item to the entry)",
    "phase4/tag-copy-reserved-destination-refusal.json": "modelled from the shipped `tag_copy` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the refusal of a reserved-provider destination under an explicit `*`)",
    "phase4/tag-copy-reserved-source-refusal.json": "modelled from the shipped `tag_copy` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the refusal of a reserved-provider source under an explicit `*`)",
    "phase4/tag-copy-sibling-denial.json": "modelled from the shipped `tag_copy` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the segment-boundary refusal of the destination (the source is exempt))",
    "phase4/tag-copy-source-missing.json": "modelled from the shipped `tag_copy` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the not_found refusal of a source that is not there)",
    "phase4/tag-copy-udt-not-allowlisted.json": "modelled from the shipped `tag_copy` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the D30 6 refusal of a UDT definition destination, under a plain Tag prefix and under `*`)",
    "phase4/tag-create-items-over-hard-limit.json": "modelled from the shipped `tag_create` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the D10 ceiling refusal is a pure request check with no Gateway involvement, so no live run can record anything the handler's own message does not already state; the live G4b stage asserts the code, reason and quoted ceiling on both rows)",
    "phase4/tag-create-no-policy.json": "modelled from the shipped `tag_create` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the shipped `tag_create` fail-closed refusal, before any policy was provisioned)",
    "phase4/tag-create-over-policy-limit.json": "modelled from the shipped `tag_create` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the D10 ceiling refusal is a pure request check with no Gateway involvement, so no live run can record anything the handler's own message does not already state; the live G4b stage asserts the code, reason and quoted ceiling on both rows)",
    "phase4/tag-create-path-over-length.json": "modelled from the shipped `tag_create` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the D10 ceiling refusal is a pure request check with no Gateway involvement, so no live run can record anything the handler's own message does not already state; the live G4b stage asserts the code, reason and quoted ceiling on both rows)",
    "phase4/tag-create-preflight-refusal.json": "modelled from the shipped `tag_create` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the whole-batch refusal, whose item list names only the refused target)",
    "phase4/tag-create-reserved-provider-refusal.json": "modelled from the shipped `tag_create` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the reserved-provider refusal under an explicit `*`)",
    "phase4/tag-create-sibling-denial.json": "modelled from the shipped `tag_create` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the segment-boundary refusal)",
    "phase4/tag-create-target-exists.json": "modelled from the shipped `tag_create` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the conflict refusal of an existing create target)",
    "phase4/tag-create-udt-not-allowlisted.json": "modelled from the shipped `tag_create` refusal shape: no live Gateway is reachable from a workstation, so the body is the handler's own code, message and `details` transcribed until the first G4b live run re-records it from the Gateway; the rehearsal replays it, and the live stage asserts the code and reason on both rows (the D30 6 refusal of a UDT definition target, under a plain Tag prefix and under `*`)",
    "phase4/tag-delete-item-keys.json": "the shipped tag_delete handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-delete-items-over-hard-limit.json": "the shipped tag_delete handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-delete-missing-target.json": "the shipped tag_delete handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-delete-no-policy.json": "the shipped tag_delete handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-delete-over-policy-limit.json": "the shipped tag_delete handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-delete-path-over-length.json": "the shipped tag_delete handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-delete-preflight-refusal.json": "the shipped tag_delete handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-delete-reserved-provider-refusal.json": "the shipped tag_delete handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-delete-sibling-denial.json": "the shipped tag_delete handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-delete-stale-fingerprint.json": "the shipped tag_delete handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-delete-udt-not-allowlisted.json": "the shipped tag_delete handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-export.json": "recorded from phase4-live-g4a run 35641086552 (8.3.8 job 106463444618); the 8.3.9 row of the same run measured identically (the provider's read-back body, replayed by the recorded Gateway fake; it also contains the harness-only oversize gate probe pair, which a deployment's provider does not have)",
    "phase4/tag-get-config-missing-template.json": "recorded from phase4-live-g4b run 35668653064 (8.3.8 job); the 8.3.9 row of the same run measured identically (the fingerprint of the same configuration and the same synthesized node), so both rows agree byte for byte apart from the run's own correlation IDs, which are templated (the node a live Gateway answers for a `tag_get_config` read of a path that is not there: `tagType: Unknown` and a native BasicTagPath, whose fingerprint is deterministic)",
    "phase4/tag-import-abort.json": "recorded from phase4-live-g4a run 35641086552 (8.3.8 job 106463444618); the 8.3.9 row of the same run measured identically",
    "phase4/tag-import-merge.json": "recorded from phase4-live-g4a run 35641086552 (8.3.8 job 106463444618); the 8.3.9 row of the same run measured identically",
    "phase4/tag-import-provider-not-ready.json": "recorded failure detail from phase4-live-g4a run 35635225572 job 106450890605 (8.3.8), where a freshly created Tag provider answered the first import while still starting",
    "phase4/tag-import-success.json": "recorded from phase4-live-g4a run 35641086552 (8.3.8 job 106463444618); the 8.3.9 row of the same run measured identically",
    "phase4/tag-move-destination-exists.json": "the shipped tag_move handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-move-destination-leaf-mismatch.json": "the shipped tag_move handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-move-items-over-hard-limit.json": "the shipped tag_move handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-move-no-policy.json": "the shipped tag_move handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-move-over-policy-limit.json": "the shipped tag_move handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-move-path-over-length.json": "the shipped tag_move handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-move-preflight-refusal.json": "the shipped tag_move handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-move-reserved-destination-refusal.json": "the shipped tag_move handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-move-reserved-source-refusal.json": "the shipped tag_move handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-move-source-missing.json": "the shipped tag_move handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-move-source-not-allowlisted.json": "the shipped tag_move handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-move-stale-fingerprint.json": "the shipped tag_move handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-move-udt-source-not-allowlisted.json": "the shipped tag_move handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-provider-create.json": "recorded from phase4-live-g4a run 35641086552 (8.3.8 job 106463444618); the 8.3.9 row of the same run measured identically; CI-only provider name, no secret material",
    "phase4/tag-provider-find.json": "recorded from phase4-live-g4a run 35641086552 (8.3.8 job 106463444618); the 8.3.9 row of the same run measured identically; carries the CI API-token actor name and the compose-network address only",
    "phase4/tag-rename-items-over-hard-limit.json": "the shipped tag_rename handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-rename-missing-target.json": "the shipped tag_rename handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-rename-new-name-not-a-segment.json": "the shipped tag_rename handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-rename-new-path-exists.json": "the shipped tag_rename handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-rename-no-policy.json": "the shipped tag_rename handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-rename-over-policy-limit.json": "the shipped tag_rename handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-rename-path-over-length.json": "the shipped tag_rename handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-rename-preflight-refusal.json": "the shipped tag_rename handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-rename-reserved-provider-refusal.json": "the shipped tag_rename handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-rename-stale-fingerprint.json": "the shipped tag_rename handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-rename-target-not-allowlisted.json": "the shipped tag_rename handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-rename-udt-not-allowlisted.json": "the shipped tag_rename handler's own canonical refusal (D06 error object), rendered from the handler's error shape and templated with the run's own paths, thresholds and correlation ID; every message and detail string is asserted to exist verbatim in the handler by tooling/native/tests/test_phase4_harness.py, and the live refusal for each case is what the first phase4-live-g4b run that exercises it records",
    "phase4/tag-update-missing-target.json": "recorded from phase4-live-g4b run 35668653064 (8.3.8 job); the 8.3.9 row of the same run measured identically (the fingerprint of the same configuration and the same synthesized node), so both rows agree byte for byte apart from the run's own correlation IDs, which are templated (the not_found refusal); the recorded refusal bodies were re-recorded after review round 1 of #10 so their `details` carry the D18 decision-audit outcome (`auditRecorded`); the live G4b run on the corrected head re-records them from the Gateway again",
    "phase4/tag-update-no-policy.json": "recorded from phase4-live-g4b run 35668653064 (8.3.8 job); the 8.3.9 row of the same run measured identically (the fingerprint of the same configuration and the same synthesized node), so both rows agree byte for byte apart from the run's own correlation IDs, which are templated (the shipped `tag_update` fail-closed refusal)",
    "phase4/tag-update-over-policy-limit.json": "modelled from the shipped `tag_update` refusal shape: the D10 item-ceiling refusal (limit_exceeded / itemsOverPolicyLimit) is a pure request check with no Gateway involvement, so no live run can record anything the handler's own message does not already state; the live G4b stage asserts the code and reason on both rows",
    "phase4/tag-update-reserved-provider-refusal.json": "recorded from phase4-live-g4b run 35668653064 (8.3.8 job); the 8.3.9 row of the same run measured identically (the fingerprint of the same configuration and the same synthesized node), so both rows agree byte for byte apart from the run's own correlation IDs, which are templated (the reserved-provider refusal under an explicit `*`); the recorded refusal bodies were re-recorded after review round 1 of #10 so their `details` carry the D18 decision-audit outcome (`auditRecorded`); the live G4b run on the corrected head re-records them from the Gateway again",
    "phase4/tag-update-sibling-denial.json": "recorded from phase4-live-g4b run 35668653064 (8.3.8 job); the 8.3.9 row of the same run measured identically (the fingerprint of the same configuration and the same synthesized node), so both rows agree byte for byte apart from the run's own correlation IDs, which are templated (the segment-boundary refusal); the recorded refusal bodies were re-recorded after review round 1 of #10 so their `details` carry the D18 decision-audit outcome (`auditRecorded`); the live G4b run on the corrected head re-records them from the Gateway again",
    "phase4/tag-update-stale-fingerprint.json": "recorded from phase4-live-g4b run 35668653064 (8.3.8 job); the 8.3.9 row of the same run measured identically (the fingerprint of the same configuration and the same synthesized node), so both rows agree byte for byte apart from the run's own correlation IDs, which are templated (the conflict refusal, whose details carry the expected and the observed fingerprint); the recorded refusal bodies were re-recorded after review round 1 of #10 so their `details` carry the D18 decision-audit outcome (`auditRecorded`); the live G4b run on the corrected head re-records them from the Gateway again",
    "phase4/tag-update-udt-not-allowlisted.json": "recorded from phase4-live-g4b run 35668653064 (8.3.8 job); the 8.3.9 row of the same run measured identically (the fingerprint of the same configuration and the same synthesized node), so both rows agree byte for byte apart from the run's own correlation IDs, which are templated (the D30 6 refusal of a UDT definition target, under `*` and under a plain Tag prefix); the recorded refusal bodies were re-recorded after review round 1 of #10 so their `details` carry the D18 decision-audit outcome (`auditRecorded`); the live G4b run on the corrected head re-records them from the Gateway again",
    "phase4/tools-list-operator.json": "the ticket #7 recorded operator inventory (run 35653953120) with the two ticket #8 Alarm Mutations added by hand: no live Gateway is reachable from the local fake, and the live alarm-shelve stage compares the deployed inventory against contracts/profiles/operator.yaml rather than against this file"
  }
}
